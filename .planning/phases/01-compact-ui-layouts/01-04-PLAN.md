---
phase: 01-compact-ui-layouts
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/App.svelte
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - The overworld campaign-start, Rift, and Factions & Troops states fit a 1366x768 desktop shell without routine page scrolling.
    - Switching from `Rifts` to `Factions & Troops` clears stale Rift-target carryover so troop assignment reads from troop context only.
    - Dense inspect combinations such as unlock plus troop hover keep the full detail header and content inside a bounded panel without losing the panel off-screen.
    - The planning shell uses its center region for real content instead of leaving a large empty band between side rails.
  artifacts:
    - path: src/ui/App.svelte
      provides: Overworld selection reset, compact planning shell sizing, and bounded dense inspect layouts for opening unlock and planning.
      contains: gameStore.setCenterMode('troops')
  key_links:
    - from: src/ui/App.svelte
      to: src/store/gameStore.ts
      via: center-mode switching that clears stale selected Rift state before troop assignment rendering
      pattern: setCenterMode\\('troops'\\)|selectedRiftId = null
    - from: src/ui/App.svelte
      to: src/ui/StatBreakdownGrid.svelte
      via: compact inspect/detail composition that keeps dense unit stats inside the left panel
      pattern: StatBreakdownGrid
---

<objective>
Close the verified overworld layout and selection-state gaps left after the original Phase 1 compaction pass.

Purpose: make the real planning states fit cleanly, remove stale cross-mode selection carryover, and eliminate the severe dense-hover overflow the UAT surfaced.
Output: a tighter, state-safe overworld layout in `src/ui/App.svelte` covering campaign start, `Rifts`, `Factions & Troops`, and unlock-detail overflow cases.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compact-ui-layouts/01-UAT.md
@.planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
@.planning/phases/01-compact-ui-layouts/01-02-SUMMARY.md
@TECHNICAL.md
@src/ui/App.svelte
@src/store/gameStore.ts
@src/ui/StatBreakdownGrid.svelte

<interfaces>
From `src/store/gameStore.ts`:

```ts
export type CenterMode = 'rifts' | 'troops';
export type ScreenMode = 'main_menu' | 'overworld' | 'replay';
```

From `src/ui/StatBreakdownGrid.svelte`:

```ts
export let stats: Array<{
  key: string;
  label: string;
  value: string;
  breakdown: StatBreakdown | null;
}>;

export let columns = 3;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair overworld mode switching so troop view resets stale Rift-target context</name>
  <files>src/ui/App.svelte</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UAT.md
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-02-SUMMARY.md
    src/ui/App.svelte
    src/store/gameStore.ts
  </read_first>
  <acceptance_criteria>
    - `src/ui/App.svelte` contains an explicit reset path that clears stale Rift-target carryover when the user enters `Factions & Troops`.
    - Troop-side assignment controls derive their selected target from troop state or fresh user choice, not a previously selected Rift from `Rifts`.
    - The `Rifts` and `Factions & Troops` toggle still uses the existing `gameStore.setCenterMode(...)` interaction model.
    - No `src/store/*`, `src/engine/*`, or `src/rendering/*` files are modified.
  </acceptance_criteria>
  <action>Update the overworld selection wiring in `src/ui/App.svelte` so switching into `Factions & Troops` clears stale Rift-selection context and the troop-assignment surface starts from troop-owned state instead of the last Rift browse target. Preserve the current three-region interaction model from `01-UI-SPEC.md` and the existing store API; this is a UI-state repair, not a store or engine refactor.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Mode switches no longer leak a selected Rift into troop inspection or assignment, and the existing store-driven overworld interaction model remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Repack the real overworld dense states to remove fit failures and dead-center space</name>
  <files>src/ui/App.svelte</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UAT.md
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-02-SUMMARY.md
    TECHNICAL.md
    src/ui/App.svelte
    src/ui/StatBreakdownGrid.svelte
  </read_first>
  <acceptance_criteria>
    - `src/ui/App.svelte` tightens the campaign-start shell so it fits with margin rather than only barely fitting.
    - `Factions & Troops` uses a denser center/detail balance that keeps troop hover or selection visible without page scroll at the common desktop target.
    - Unlock-plus-troop-hover inspect states are bounded inside the left inspect panel, with the troop heading and body remaining visible together.
    - The planning shell reduces empty middle-space reservations between left and right rails in favor of content width for the active browse region.
    - Overflow remains bounded to internal support panels instead of reintroducing page-shell scrolling or new modal navigation.
  </acceptance_criteria>
  <action>Retune the overworld layout and inspect CSS in `src/ui/App.svelte` for the exact failed UAT states: campaign start, troop browsing with hover or pinned detail, and unlock/troop inspect overlap. Shift width and height budget from decorative gaps and dead center bands into the active browse column, and cap dense inspect sections so the full detail header plus content stays inside the persistent panel. Preserve the approved Phase 1 visual language and existing hover-preview plus pin behavior instead of adding drawers, popups, or a new navigation path.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The overworld shell fits its real dense planning states more reliably, keeps the persistent inspect panel usable, and no longer wastes central layout space that should belong to active content.</done>
</task>

</tasks>

<verification>
Run `npm run build` after each task. The resulting source should show both a troop-mode selection reset and tighter planning-shell fit rules for the failed dense states called out in `01-UAT.md`.
</verification>

<success_criteria>
The failed overworld UAT truth is closed: campaign start, Rift browse, and Factions & Troops all fit more reliably on common desktop screens, dense inspect states stay bounded, and switching modes no longer carries stale Rift-target state into troop view.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compact-ui-layouts/01-04-SUMMARY.md`
</output>
