---
phase: 01-compact-ui-layouts
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.css
  - src/ui/App.svelte
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - Core gameplay screens share one compact shell rhythm instead of oversized padding and inconsistent card chrome.
    - Gameplay panels collapse to content height or bounded internal scroll instead of forcing shell-level dead space.
    - The existing menu, overworld, and replay navigation model stays intact while density improves.
  artifacts:
    - path: src/app.css
      provides: Shared compact spacing, typography, and color tokens aligned to the approved UI spec.
      contains: Alegreya Sans SC
    - path: src/ui/App.svelte
      provides: Shared shell, topbar, panel, card, and overflow rules reused by Phase 1 screens.
      contains: .shell
  key_links:
    - from: src/app.css
      to: src/ui/App.svelte
      via: shared spacing and typography variables consumed by shell styles
      pattern: var\\(--ui-
    - from: src/ui/App.svelte
      to: .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
      via: shell widths, padding, and overflow choices
      pattern: 320px|minmax\\(0, 1fr\\)|overflow
---

<objective>
Establish the compact layout baseline for Phase 1 without changing navigation, store flow, or renderer ownership.

Purpose: reduce the layout blast radius in `App.svelte` by locking shared density rules before screen-specific tuning.
Output: approved spacing/typography tokens in `src/app.css` and tighter shared shell, panel, and overflow rules in `src/ui/App.svelte`.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compact-ui-layouts/01-RESEARCH.md
@.planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
@TECHNICAL.md
@src/app.css
@src/ui/App.svelte

<interfaces>
From `src/store/gameStore.ts`:

```ts
export type CenterMode = 'rifts' | 'troops';
export type ScreenMode = 'main_menu' | 'overworld' | 'replay';
```

From `src/store/saveSlots.ts`:

```ts
export interface SaveSlotSummary {
  slotId: SaveSlotId;
  status: 'empty' | 'occupied';
  cycleNumber: number | null;
  phase: CampaignPhase | null;
  factionLabel: string | null;
  lastPlayedAt: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the UI-spec spacing and typography contract into shared CSS tokens</name>
  <files>src/app.css</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-RESEARCH.md
    src/app.css
    src/ui/App.svelte
  </read_first>
  <acceptance_criteria>
    - `src/app.css` defines compact spacing variables for the approved `4px / 8px / 16px / 24px / 32px` scale.
    - `src/app.css` keeps the current Alegreya Sans SC and IBM Plex Mono font pairing.
    - `src/app.css` exposes shared surface and accent variables matching the dark steel-and-brass palette from `01-UI-SPEC.md`.
    - `src/app.css` does not introduce a new drawer/modal design system or navigation tokens unrelated to Phase 1 density work.
  </acceptance_criteria>
  <action>Add a small shared token layer in `src/app.css` for the approved spacing, typography, shell width, panel radius, and color values from `01-UI-SPEC.md`. Keep the existing visual language and fonts, but normalize the values so later screen work can tighten layout by consuming one compact token set instead of repeating ad hoc numbers through `App.svelte`. Do not add app-state logic, store changes, or new global resets unrelated to the brownfield UI phase.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`src/app.css` becomes the single source of truth for compact Phase 1 density tokens without changing how the app boots or how gameplay logic is owned.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the shared shell, panel, and overflow rules in App.svelte</name>
  <files>src/ui/App.svelte</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-RESEARCH.md
    TECHNICAL.md
    src/app.css
    src/ui/App.svelte
  </read_first>
  <acceptance_criteria>
    - `src/ui/App.svelte` reduces shell-level padding and gap values to the compact scale from `01-UI-SPEC.md`.
    - Shared panel chrome in `src/ui/App.svelte` uses the new token values rather than repeating large one-off padding and radius values.
    - Shared gameplay shells prefer bounded internal overflow for dense regions instead of page-level growth.
    - The existing `main_menu`, `overworld`, and `replay` screen branches remain the active navigation model.
    - No `src/store/*`, `src/engine/*`, or `src/rendering/*` files are modified in this plan.
  </acceptance_criteria>
  <action>Refactor the shared layout CSS inside `src/ui/App.svelte` to consume the compact tokens and the locked shell sizing from `01-UI-SPEC.md`. Tighten `.shell`, `.replay-shell`, `.topbar`, `.panel`, shared card grids, and common overflow wrappers before any screen-specific markup changes. Preserve the current screen branches and interaction model exactly; this plan is only the baseline compaction pass per the brownfield contract.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The app has one coherent compact shell baseline that later menu/overworld and replay work can build on without reopening broad spacing decisions.</done>
</task>

</tasks>

<verification>
Run `npm run build` after each task. The resulting source should show shared token usage and tighter shell rules before any screen-specific compaction starts.
</verification>

<success_criteria>
Phase 1 has a stable density baseline: spacing and panel chrome are centralized, the existing screen model is preserved, and later plans can focus on menu/overworld and replay fit instead of re-litigating global shell decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compact-ui-layouts/01-01-SUMMARY.md`
</output>
