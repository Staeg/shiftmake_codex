---
phase: 01-compact-ui-layouts
plan: 2
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/ui/App.svelte
  - src/ui/StatBreakdownGrid.svelte
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - The main menu, opening unlock, and overworld planning screens fit a common desktop viewport without routine page scroll.
    - Rift, troop, offer, and archive details stay visible in persistent side regions instead of oversized popups.
    - Topbar, inspect, browse, archive, and end-cycle surfaces remain visible together in the planning flow outside dense edge cases.
  artifacts:
    - path: src/ui/App.svelte
      provides: Compact menu, opening unlock, and overworld planning layouts built on the Phase 1 shell baseline.
      contains: opening_unlock
    - path: src/ui/StatBreakdownGrid.svelte
      provides: Denser reusable stat-card presentation for inspect surfaces.
      contains: stats-grid
  key_links:
    - from: src/ui/App.svelte
      to: src/ui/StatBreakdownGrid.svelte
      via: dense inspect and troop detail stat rendering
      pattern: StatBreakdownGrid
    - from: src/ui/App.svelte
      to: src/store/gameStore.ts
      via: preserved center-mode and end-cycle action wiring
      pattern: gameStore\\.
---

<objective>
Compact the menu and overworld flows so planning-critical information stays on one desktop screen without changing the existing left/center/right interaction model.

Purpose: satisfy the main gameplay-side Phase 1 fit requirements first, where the user makes campaign decisions and inspects rifts, troops, offers, and archive entries.
Output: denser `main_menu`, `opening_unlock`, and `planning` layouts in `src/ui/App.svelte`, plus compact stat-card treatment in `src/ui/StatBreakdownGrid.svelte`.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-compact-ui-layouts/01-RESEARCH.md
@.planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
@TECHNICAL.md
@src/ui/App.svelte
@src/ui/StatBreakdownGrid.svelte
@src/store/gameStore.ts

<interfaces>
From `src/store/gameStore.ts`:

```ts
export type CenterMode = 'rifts' | 'troops';
export type ScreenMode = 'main_menu' | 'overworld' | 'replay';
```

From `src/ui/StatBreakdownGrid.svelte`:

```ts
export let stats: Array<{
  key: string;
  label: string;
  value: string;
  breakdown: StatBreakdown | null;
}>;

export let columns = 3;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact the main menu and opening-unlock screens around persistent inspection</name>
  <files>src/ui/App.svelte</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-RESEARCH.md
    src/ui/App.svelte
    src/store/saveSlots.ts
  </read_first>
  <acceptance_criteria>
    - `main_menu` keeps all three save slots and primary actions visible in one centered screen layout at the common desktop target.
    - Save-slot copy is reduced to a compact supporting block instead of consuming major vertical space.
    - `opening_unlock` preserves the existing left-detail and right-choice model instead of switching to a popup or new navigation mode.
    - The opening inspect area stays persistent on desktop and supports hover or pinned preview without a new modal.
    - `Replace Save` remains visually secondary to `Load Slot` and `Start Campaign`.
  </acceptance_criteria>
  <action>Update the menu and opening-unlock markup/styles in `src/ui/App.svelte` to follow the locked UI spec: one-screen slot visibility, denser introductory copy, and a persistent inspect-plus-choice layout for the opening pick. Tighten faction card, troop icon, and preview spacing before hiding any information. Do not introduce drawers, route changes, or new popup-driven inspection patterns.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The first-play and save-slot flows are visibly denser and remain readable without requiring routine vertical scroll on a 1366x768 desktop target.</done>
</task>

<task type="auto">
  <name>Task 2: Rebalance the overworld planning shell for single-screen browsing and action visibility</name>
  <files>src/ui/App.svelte, src/ui/StatBreakdownGrid.svelte</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-RESEARCH.md
    src/ui/App.svelte
    src/ui/StatBreakdownGrid.svelte
    src/store/gameStore.ts
  </read_first>
  <acceptance_criteria>
    - The overworld planning view keeps the existing three-region model with a compact `left / center / right` desktop balance.
    - The topbar stays on one row at common desktop sizes with title, resource strip, and mode actions visible together.
    - Rift cards emphasize tier or id, VP, mutators, assigned troops, and one direct action before decorative spacing.
    - Troop mode keeps troop identity, readiness or assignment state, core stats, abilities, and assignment targets visible in one view.
    - Archive inspection remains in the right column and uses bounded internal scroll instead of opening a new navigation mode.
    - Dense stat surfaces use `src/ui/StatBreakdownGrid.svelte` rather than inventing a second compact stat layout.
  </acceptance_criteria>
  <action>Refine the overworld branch in `src/ui/App.svelte` around the approved `320px / flexible / 320px`-style layout target. Tighten the topbar, rift cards, troop cards, support/archive rail, and action rail so the main browse region remains dominant and the inspect/archive panels become compact support surfaces. Use `StatBreakdownGrid.svelte` to reduce detail-card dead space where it materially improves fit, but keep all gameplay logic and state ownership where they already live.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The planning screen shows decision-critical information and related actions together with bounded internal scrolling only in dense subpanels such as archive or offers.</done>
</task>

</tasks>

<verification>
Run `npm run build` after each task. During execution, confirm the source still uses the existing `gameStore` actions and screen branches rather than introducing a replacement planning flow.
</verification>

<success_criteria>
Players can navigate the main menu, opening unlock, and overworld planning screens through tighter single-screen desktop layouts that keep inspection, selection, archive, and end-cycle actions visible together.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compact-ui-layouts/01-02-SUMMARY.md`
</output>
