---
phase: 01-compact-ui-layouts
plan: 5
type: execute
wave: 2
depends_on:
  - 01-04
files_modified:
  - src/ui/App.svelte
  - src/ui/BattleControls.svelte
  - src/ui/EventLog.svelte
  - src/ui/UnitTooltip.svelte
  - src/rendering/BattleRenderer.ts
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - Replay zoom and transport controls fit the compact shell without a visibly oversized zoom cluster.
    - The battlefield remains dominant while dead space below the board is removed from the replay shell.
    - Clicking a unit from the board, alive-count roster, or recap follows the same lock-focus rule from the UI spec.
    - Timeline or highlighted-flow selection can preview and highlight units without creating a different, confusing lock model.
  artifacts:
    - path: src/ui/App.svelte
      provides: Compact replay shell layout plus unified replay focus and lock coordination.
      contains: lockedUnitId
    - path: src/rendering/BattleRenderer.ts
      provides: Board-click interaction contract for replay unit focus.
      contains: onUnitClick
    - path: src/ui/BattleControls.svelte
      provides: Compact transport controls sized for the replay shell.
      contains: Reset Replay
  key_links:
    - from: src/rendering/BattleRenderer.ts
      to: src/ui/App.svelte
      via: onUnitClick and hover handlers feeding the shared replay lock state
      pattern: onUnitClick|lockedUnitId
    - from: src/ui/App.svelte
      to: src/ui/BattleControls.svelte
      via: replay control rail sizing and labels under the battlefield
      pattern: BattleControls
    - from: src/ui/App.svelte
      to: src/ui/EventLog.svelte
      via: selected event highlighting without introducing a separate lock behavior
      pattern: selectedEvent|EventLog
---

<objective>
Close the replay UAT gaps around oversized control chrome, wasted vertical space, and inconsistent click-lock behavior.

Purpose: make replay interaction read as one coherent focus model while preserving the Phase 1 compact shell and battlefield-first visual contract.
Output: a tighter replay shell and unified focus-lock contract across `src/ui/App.svelte`, `BattleControls.svelte`, `EventLog.svelte`, `UnitTooltip.svelte`, and `BattleRenderer.ts`.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compact-ui-layouts/01-UAT.md
@.planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
@.planning/phases/01-compact-ui-layouts/01-03-SUMMARY.md
@TECHNICAL.md
@src/ui/App.svelte
@src/ui/BattleControls.svelte
@src/ui/EventLog.svelte
@src/ui/UnitTooltip.svelte
@src/rendering/BattleRenderer.ts

<interfaces>
From `src/ui/BattleControls.svelte`:

```ts
export let replayLength = 0;
export let currentStep = -1;
export let autoPlay = false;
export let speedMs = 500;
export let onStepBack: () => void;
export let onStepForward: () => void;
export let onJumpStart: () => void;
export let onToggleAuto: () => void;
export let onSetSpeed: (ms: number) => void;
```

From `src/rendering/BattleRenderer.ts`:

```ts
export type UnitPointerInfo = {
  unitId: string;
  x: number;
  y: number;
};

export class BattleRenderer {
  setInteractionHandlers(handlers: BattleInteractionHandlers): void;
  resetZoom(): void;
  setHighlights(strongIds: string[], faintIds: string[]): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shrink replay control chrome and reclaim wasted vertical space under the battlefield</name>
  <files>src/ui/App.svelte, src/ui/BattleControls.svelte, src/ui/EventLog.svelte, src/ui/UnitTooltip.svelte</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UAT.md
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-03-SUMMARY.md
    src/ui/App.svelte
    src/ui/BattleControls.svelte
    src/ui/EventLog.svelte
    src/ui/UnitTooltip.svelte
  </read_first>
  <acceptance_criteria>
    - The replay zoom cluster in `src/ui/App.svelte` uses a visibly tighter footprint than the current `+ / - / Reset Zoom` group.
    - The replay shell reduces reserved vertical space below the battlefield instead of leaving a dead band under the main display.
    - `src/ui/BattleControls.svelte` remains explicit-label driven and compact enough to sit under the viewport without crowding the shell.
    - `src/ui/EventLog.svelte` and `src/ui/UnitTooltip.svelte` stay bounded and compact; no new modal or drawer system is introduced.
    - Replay controls keep the required accessible labels from `01-UI-SPEC.md`.
  </acceptance_criteria>
  <action>Refine the replay shell sizing in `src/ui/App.svelte` and compact the supporting chrome in `BattleControls.svelte`, `EventLog.svelte`, and `UnitTooltip.svelte` so the battlefield uses more of the available height and the zoom cluster no longer dominates the viewport corner. Reduce decorative padding and control bulk before hiding information, and keep the explicit-label accessibility contract intact per `01-UI-SPEC.md`.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The replay shell looks materially tighter, the board area loses the dead space under it, and the support surfaces still remain readable and bounded.</done>
</task>

<task type="auto">
  <name>Task 2: Unify replay unit selection so board clicks, roster clicks, and recap clicks follow one lock-focus rule</name>
  <files>src/ui/App.svelte, src/rendering/BattleRenderer.ts</files>
  <read_first>
    .planning/phases/01-compact-ui-layouts/01-UAT.md
    .planning/phases/01-compact-ui-layouts/01-UI-SPEC.md
    .planning/phases/01-compact-ui-layouts/01-03-SUMMARY.md
    TECHNICAL.md
    src/ui/App.svelte
    src/rendering/BattleRenderer.ts
  </read_first>
  <acceptance_criteria>
    - `src/ui/App.svelte` has one shared replay lock-state path used by board clicks and non-board unit-selection actions.
    - Board click, alive-count cycling, and recap-unit selection all set or clear focus through the same lock semantics instead of mixing lock and preview-only behaviors.
    - Timeline or highlighted-flow selection continues to drive renderer highlights without silently creating a conflicting lock state.
    - `src/rendering/BattleRenderer.ts` remains presentation-only and does not gain gameplay logic or replay-store ownership.
  </acceptance_criteria>
  <action>Consolidate replay unit-focus handling in `src/ui/App.svelte` so the UI spec's click-lock rule is enforced consistently across board selection, alive roster selection, and recap-unit selection. Adjust the `BattleRenderer.ts` interaction contract only as needed to feed that shared lock path cleanly; keep event-log and highlighted-step behavior as highlight or preview signals unless the user performs a true click-selection action. Do not move replay state ownership into the renderer or introduce a second selection model.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Replay focus behavior is coherent: clicking a unit anywhere that represents explicit unit selection follows the same lock rule, while timeline highlighting remains a separate visual cue rather than a competing focus model.</done>
</task>

</tasks>

<verification>
Run `npm run build` after each task. The resulting source should show both a smaller replay control footprint and a single replay unit-lock path spanning board and non-board unit selection.
</verification>

<success_criteria>
The failed replay UAT truth is closed: the replay shell is tighter, the battlefield has less wasted vertical space around it, and unit selection behavior reads consistently with the UI spec's click-lock rule.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compact-ui-layouts/01-05-SUMMARY.md`
</output>
