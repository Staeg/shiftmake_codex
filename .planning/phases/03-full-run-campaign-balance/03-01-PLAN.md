---
phase: 03-full-run-campaign-balance
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/campaignSimulation.ts
  - src/engine/campaignSimulation.test.ts
  - scripts/reportCampaignBalance.ts
autonomous: true
requirements:
  - BAL-01
  - BAL-02
  - BAL-03
  - BAL-04
  - BAL-05
must_haves:
  truths:
    - Phase 3 has a deterministic campaign-level measurement harness instead of relying on one-off manual runs.
    - Balance changes can be evaluated across multiple seeds and scripted play policies before and after tuning.
    - A reproducible report can summarize final VP, wins, losses, owned factions, troop growth, and unused Essence by policy.
  artifacts:
    - path: src/engine/campaignSimulation.ts
      provides: Campaign sweep helpers, policy definitions, and summary metrics.
      contains: export function simulateCampaign
    - path: src/engine/campaignSimulation.test.ts
      provides: Deterministic tests for single-run and sweep metrics.
      contains: describe('campaignSimulation')
    - path: scripts/reportCampaignBalance.ts
      provides: Reproducible campaign balance report generation.
      contains: reportCampaignBalance
  key_links:
    - from: src/engine/campaignSimulation.ts
      to: src/engine/game.ts
      via: full-cycle campaign progression under scripted policies
      pattern: applyCycleOutcomes
    - from: src/engine/campaignSimulation.ts
      to: src/engine/rift.ts
      via: generated Rift schedules and outcomes over many seeds
      pattern: generateCycleRifts
    - from: src/engine/campaignSimulation.test.ts
      to: src/engine/campaignSimulation.ts
      via: deterministic sweep assertions
      pattern: sweepCampaignSeeds
---

<objective>
Create the measurement layer for Phase 3 so later tuning is driven by deterministic campaign evidence instead of intuition alone.

Purpose: give the executor a repeatable way to evaluate early pressure, late tension, Essence flow, and unlock variety across many seeds.
Output: a campaign simulation harness, campaign-sweep tests, and a report script under `scripts/`.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
@.planning/phases/03-full-run-campaign-balance/03-VALIDATION.md
@TECHNICAL.md
@src/engine/game.ts
@src/engine/rift.ts
@src/engine/simulationHarness.ts
@src/engine/campaign.test.ts
@src/engine/permutationReport.ts

<interfaces>
New engine module should expose:

```ts
export type CampaignPolicyId = 'greedy-vp' | 'greedy-growth' | 'balanced';

export interface CampaignSimulationMetrics {
  finalVictoryPoints: number;
  victories: number;
  defeats: number;
  finalEssence: number;
  ownedFactions: number;
  ownedTroops: number;
  highestTierDefeated: number;
}

export function simulateCampaign(seed: number, policyId: CampaignPolicyId): CampaignSimulationMetrics;
export function sweepCampaignSeeds(policyId: CampaignPolicyId, seeds: number[]): CampaignSweepResult;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic campaign simulation helpers and report metrics</name>
  <files>src/engine/campaignSimulation.ts</files>
  <read_first>
    .planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
    .planning/phases/03-full-run-campaign-balance/03-VALIDATION.md
    src/engine/game.ts
    src/engine/rift.ts
    src/engine/simulationHarness.ts
    src/engine/permutationReport.ts
    TECHNICAL.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/campaignSimulation.ts` contains `export type CampaignPolicyId =`.
    - `src/engine/campaignSimulation.ts` contains `'greedy-vp'`.
    - `src/engine/campaignSimulation.ts` contains `'greedy-growth'`.
    - `src/engine/campaignSimulation.ts` contains `'balanced'`.
    - `src/engine/campaignSimulation.ts` contains `export function simulateCampaign`.
    - `src/engine/campaignSimulation.ts` contains `export function sweepCampaignSeeds`.
    - `src/engine/campaignSimulation.ts` contains `finalVictoryPoints`.
    - `src/engine/campaignSimulation.ts` contains `highestTierDefeated`.
  </acceptance_criteria>
  <action>Create `src/engine/campaignSimulation.ts` as a pure TypeScript engine helper. Add the exact policy IDs `greedy-vp`, `greedy-growth`, and `balanced`. Implement `simulateCampaign(seed, policyId)` by starting a game, claiming an opening troop, auto-claiming offers with deterministic policy rules, assigning ready troops to discovered Rifts with deterministic target ranking, resolving through cycle 10, and returning metrics including `finalVictoryPoints`, `victories`, `defeats`, `finalEssence`, `ownedFactions`, `ownedTroops`, `highestTierDefeated`, `cycleReached`, and `unusedEssenceByCycle`. Implement `sweepCampaignSeeds(policyId, seeds)` to aggregate averages and percentile-style summaries across multiple full runs.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Phase 3 has a deterministic, engine-owned way to measure full-run pacing across many seeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add campaign-sweep regression tests for policies and summary metrics</name>
  <files>src/engine/campaignSimulation.test.ts</files>
  <read_first>
    src/engine/campaignSimulation.ts
    src/engine/campaign.test.ts
    src/engine/simulationHarness.test.ts
    .planning/phases/03-full-run-campaign-balance/03-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/campaignSimulation.test.ts` contains `describe('campaignSimulation'`.
    - `src/engine/campaignSimulation.test.ts` contains `simulateCampaign`.
    - `src/engine/campaignSimulation.test.ts` contains `sweepCampaignSeeds`.
    - `src/engine/campaignSimulation.test.ts` contains `'greedy-vp'`.
    - `src/engine/campaignSimulation.test.ts` contains `'balanced'`.
    - `npm run test` exits with only known unrelated failures if Phase 2 is still pending, and otherwise exits successfully.
  </acceptance_criteria>
  <behavior>
    - Single-run tests prove campaign simulations are deterministic for a fixed seed and policy.
    - Sweep tests prove aggregate summaries include at least VP, defeats, owned factions, and unused Essence.
    - Policy tests prove `greedy-growth` and `balanced` do not collapse to identical decisions on the same seed.
  </behavior>
  <action>Create `src/engine/campaignSimulation.test.ts` with focused deterministic tests: one test that `simulateCampaign(101, 'balanced')` returns stable metrics across repeated calls, one test that `sweepCampaignSeeds('greedy-vp', createSeedRange(6, 500))` returns six entries and aggregate summary fields, and one test that `greedy-growth` and `balanced` differ on at least one tracked metric for the same fixed seed. Keep assertions exact on fields and shapes, not subjective on “good balance.”</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The new harness is protected by deterministic tests and ready to support balance tuning.</done>
</task>

<task type="auto">
  <name>Task 3: Add a reproducible campaign balance report script</name>
  <files>scripts/reportCampaignBalance.ts</files>
  <read_first>
    scripts/reportPermutations2v2.ts
    scripts/reportPermutations3v3.ts
    scripts/reportPermutationsCommon.ts
    src/engine/campaignSimulation.ts
    .planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `scripts/reportCampaignBalance.ts` contains `reportCampaignBalance`.
    - `scripts/reportCampaignBalance.ts` contains `greedy-vp`.
    - `scripts/reportCampaignBalance.ts` contains `greedy-growth`.
    - `scripts/reportCampaignBalance.ts` contains `balanced`.
    - `scripts/reportCampaignBalance.ts` contains `balance_results/campaign`.
  </acceptance_criteria>
  <action>Create `scripts/reportCampaignBalance.ts` following the existing report-script pattern. Accept CLI flags `--runs=`, `--startSeed=`, `--policy=`, and `--outputDir=`. Default the output directory to `balance_results/campaign`. Produce markdown or JSON output that lists per-policy averages for final VP, victories, defeats, owned factions, owned troops, highest tier defeated, and average unused Essence. Name the top-level function `reportCampaignBalance` so later plans can reuse it.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Phase 3 can generate baseline and post-tuning balance reports without ad-hoc manual logging.</done>
</task>

</tasks>

<verification>
Run `npm run test` after each task. If Phase 2 is still unimplemented and its known role-behavior tests fail, record that as an external precondition rather than weakening the new Phase 3 assertions.
</verification>

<success_criteria>
Phase 3 now has a campaign-level measurement harness and report path, making later tuning evidence-driven and reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-run-campaign-balance/03-01-SUMMARY.md`
</output>
