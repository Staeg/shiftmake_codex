---
phase: 03-full-run-campaign-balance
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/engine/army.ts
  - src/engine/rift.ts
  - src/engine/rift.test.ts
autonomous: true
requirements:
  - BAL-01
  - BAL-02
  - BAL-03
must_haves:
  truths:
    - Rift difficulty grows more smoothly from cycle 1 through cycle 10 instead of spiking early and flattening later.
    - Enemy armies always include a readable role shape appropriate to their tier instead of arbitrary all-flex compositions.
    - Saturation and enemy stat growth stay inside bounded, testable tables.
  artifacts:
    - path: src/engine/rift.ts
      provides: Explicit schedule, saturation bands, and role-quota enemy generation.
      contains: TIER_ROLE_SLOTS
    - path: src/engine/army.ts
      provides: Tier multiplier table for enemy combatant scaling.
      contains: ENEMY_TIER_MULTIPLIERS
    - path: src/engine/rift.test.ts
      provides: Regression coverage for schedule, saturation, composition, and scaling.
      contains: describe('rift generation')
  key_links:
    - from: src/engine/rift.ts
      to: src/engine/army.ts
      via: enemy generation uses tier-aware stat scaling
      pattern: resolveEnemyCombatant
    - from: src/engine/rift.test.ts
      to: src/engine/rift.ts
      via: deterministic generation assertions
      pattern: generateCycleRifts
---

<objective>
Rebalance the campaign's threat curve by replacing the current flat-late, high-variance Rift generation with explicit schedule, composition, and scaling tables.

Purpose: satisfy BAL-01 through BAL-03 with deterministic difficulty shaping rather than random spike mitigation.
Output: updated cycle schedule, bounded saturation, role-aware enemy composition, adjusted tier multipliers, and regression tests.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
@.planning/phases/03-full-run-campaign-balance/03-VALIDATION.md
@src/engine/army.ts
@src/engine/rift.ts
@src/engine/rift.test.ts
@src/engine/campaignSimulation.ts
@design documents/Rifts.md

<interfaces>
Phase 3 should keep the existing public entrypoints:

```ts
export function generateCycleRifts(state: Pick<GameState, 'campaignSeed' | 'cycleNumber'>): RiftInstance[];
export function resolveEnemyCombatant(...): ResolvedCombatantDefinition;
```

Tune them through internal tables and helpers, not API churn.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the current schedule and saturation randomness with explicit tier tables</name>
  <files>src/engine/rift.ts</files>
  <read_first>
    src/engine/rift.ts
    src/engine/rift.test.ts
    .planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
    design documents/Rifts.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/rift.ts` contains `[1, 1, 1, 1]`.
    - `src/engine/rift.ts` contains `[4, 4, 3, 2]`.
    - `src/engine/rift.ts` contains `const TIER_SATURATION_BANDS`.
    - `src/engine/rift.ts` contains `5, 7`.
    - `src/engine/rift.ts` contains `8, 10`.
    - `src/engine/rift.ts` still contains `export function generateCycleRifts`.
  </acceptance_criteria>
  <action>Update `src/engine/rift.ts` so `CYCLE_RIFT_TIERS` uses the exact 10-cycle table from research: cycle 1 `[1,1,1,1]`, cycle 2 `[2,1,1,1]`, cycle 3 `[2,2,1,1]`, cycle 4 `[2,2,2,1]`, cycle 5 `[3,2,2,1]`, cycle 6 `[3,3,2,1]`, cycle 7 `[3,3,2,2]`, cycle 8 `[4,3,2,2]`, cycle 9 `[4,3,3,2]`, cycle 10+ `[4,4,3,2]`. Replace the current saturation `3 + rng.int(13)` with exact tier bands `{1:[5,7],2:[6,8],3:[7,9],4:[8,10]}` and sample only inside the matching band.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The campaign curve has an explicit schedule and bounded battlefield density instead of a broad random range.</done>
</task>

<task type="auto">
  <name>Task 2: Make enemy Rift armies follow role quotas and bounded tier multipliers</name>
  <files>src/engine/rift.ts, src/engine/army.ts</files>
  <read_first>
    src/engine/rift.ts
    src/engine/army.ts
    src/engine/unitCatalog.ts
    .planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
    TECHNICAL.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/rift.ts` contains `const TIER_ROLE_SLOTS`.
    - `src/engine/rift.ts` contains `'frontline'`.
    - `src/engine/rift.ts` contains `'backline'`.
    - `src/engine/rift.ts` contains `'chaff'`.
    - `src/engine/army.ts` contains `const ENEMY_TIER_MULTIPLIERS`.
    - `src/engine/army.ts` contains `1.08`.
    - `src/engine/army.ts` contains `1.3`.
    - `src/engine/army.ts` contains `1.06`.
  </acceptance_criteria>
  <behavior>
    - Tier 1 armies contain `frontline, flex`.
    - Tier 2 armies contain `frontline, backline, flex`.
    - Tier 3 armies contain `frontline, chaff, backline, flex`.
    - Tier 4 armies contain `frontline, chaff, backline, flex, flex`.
    - Tier multipliers are health `1/1.08/1.18/1.3`, damage `1/1.05/1.12/1.2`, speed `1/1.02/1.04/1.06`.
  </behavior>
  <action>Refactor enemy army generation so `buildEnemyArmy()` fills exact role-slot templates: tier 1 `['frontline','flex']`, tier 2 `['frontline','backline','flex']`, tier 3 `['frontline','chaff','backline','flex']`, tier 4 `['frontline','chaff','backline','flex','flex']`. Select unique non-summoned unlocks matching each slot when possible, fall back only when a role pool is empty, and cap summoner picks at one for tiers 1-2 and two for tiers 3-4. In `src/engine/army.ts`, replace the current uniform `+10%` scaling with exact multiplier tables: health `{1:1,2:1.08,3:1.18,4:1.3}`, damage `{1:1,2:1.05,3:1.12,4:1.2}`, speed `{1:1,2:1.02,3:1.04,4:1.06}`.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Higher-tier Rifts escalate through clearer role shape and gentler, bounded stat growth instead of arbitrary random spikes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the new schedule, composition, and scaling rules into rift tests</name>
  <files>src/engine/rift.test.ts</files>
  <read_first>
    src/engine/rift.test.ts
    src/engine/rift.ts
    src/engine/army.ts
    src/engine/campaignSimulation.ts
    .planning/phases/03-full-run-campaign-balance/03-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/rift.test.ts` contains `uses the full ten-cycle schedule`.
    - `src/engine/rift.test.ts` contains `uses tier-bounded saturation bands`.
    - `src/engine/rift.test.ts` contains `role quota`.
    - `src/engine/rift.test.ts` contains `tier multipliers`.
    - `npm run test` exits with only known unrelated failures if Phase 2 is still pending, and otherwise exits successfully.
  </acceptance_criteria>
  <action>Extend `src/engine/rift.test.ts` with concrete assertions for the new schedule table, the exact saturation bands for each tier, the presence of required roles per tier-generated army, and the exact enemy stat multipliers at tiers 2, 3, and 4. Keep the tests deterministic and table-driven; do not rely on broad “harder than before” assertions.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The new Rift balance contract is captured in deterministic regression tests.</done>
</task>

</tasks>

<verification>
Run `npm run test` after each task. If a composition test becomes brittle, tighten the generator rules instead of weakening the contract.
</verification>

<success_criteria>
Rift escalation is now smooth, role-shaped, and deterministic enough to support fair challenge from cycle 1 through cycle 10.
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-run-campaign-balance/03-02-SUMMARY.md`
</output>
