---
phase: 03-full-run-campaign-balance
plan: 3
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/engine/game.ts
  - src/engine/campaign.test.ts
  - src/engine/campaignSimulation.ts
autonomous: true
requirements:
  - BAL-04
  - BAL-05
must_haves:
  truths:
    - The player economy distinguishes troop growth from global upgrade growth instead of pricing both identically from cycle 1.
    - Early troop offers reliably expose new-faction options until the player has a readable base roster.
    - Campaign sweeps can measure owned factions, unused Essence, and progression shape after the new economy rules.
  artifacts:
    - path: src/engine/game.ts
      provides: Tuned Essence rewards, offer costs, and reveal gating.
      contains: UPGRADE_OFFER_COST
    - path: src/engine/campaign.test.ts
      provides: Deterministic coverage for bonus Essence, upgrade gating, and offer priority.
      contains: first-victory Essence bonus
    - path: src/engine/campaignSimulation.ts
      provides: Policy metrics for owned factions and unused Essence after pacing changes.
      contains: ownedFactions
  key_links:
    - from: src/engine/game.ts
      to: src/engine/campaignSimulation.ts
      via: campaign policies observe new economy and offer behavior
      pattern: revealUpgradeOffer
    - from: src/engine/campaign.test.ts
      to: src/engine/game.ts
      via: deterministic progression-rule assertions
      pattern: claimUpgradeOffer
---

<objective>
Rebalance player progression so roster growth, upgrade timing, and Essence flow stay readable across a full run.

Purpose: satisfy BAL-04 and BAL-05 with concrete economy rules instead of flat per-cycle growth and immediate upgrade access.
Output: tuned claim costs, first-win reward logic, delayed upgrade access, early new-faction troop priority, and regression coverage.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
@.planning/phases/03-full-run-campaign-balance/03-VALIDATION.md
@src/engine/game.ts
@src/engine/campaign.test.ts
@src/engine/campaignSimulation.ts
@design documents/Overworld.md

<interfaces>
Keep these public functions stable:

```ts
export function revealTroopOffer(state: GameState): GameState;
export function revealUpgradeOffer(state: GameState): GameState;
export function claimTroopOffer(state: GameState, troopUnlockId: TroopUnlockId): GameState;
export function claimUpgradeOffer(state: GameState, upgradeId: UpgradeId): GameState;
export function applyCycleOutcomes(state: GameState, resolution: CycleResolution): ApplyCycleOutcomeResult;
```

Implement tuning through constants and helper functions inside `game.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate troop and upgrade pacing with exact costs, reward bonuses, and reveal gating</name>
  <files>src/engine/game.ts</files>
  <read_first>
    src/engine/game.ts
    src/engine/campaign.test.ts
    .planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
    design documents/Overworld.md
    TECHNICAL.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/game.ts` contains `const TROOP_OFFER_COST = 1`.
    - `src/engine/game.ts` contains `const UPGRADE_OFFER_COST = 2`.
    - `src/engine/game.ts` contains `const FIRST_VICTORY_ESSENCE_BONUS = 1`.
    - `src/engine/game.ts` contains `const MIN_TROOPS_FOR_UPGRADES = 3`.
    - `src/engine/game.ts` contains `state.troops.length < MIN_TROOPS_FOR_UPGRADES`.
    - `src/engine/game.ts` contains `record.outcome === 'victory'`.
  </acceptance_criteria>
  <action>Add explicit progression constants to `src/engine/game.ts`: `TROOP_OFFER_COST = 1`, `UPGRADE_OFFER_COST = 2`, `FIRST_VICTORY_ESSENCE_BONUS = 1`, and `MIN_TROOPS_FOR_UPGRADES = 3`. Keep opening and base end-of-cycle Essence at `2`. Change troop claims to spend `TROOP_OFFER_COST`, change upgrade claims to spend `UPGRADE_OFFER_COST`, block `revealUpgradeOffer()` while `state.troops.length < MIN_TROOPS_FOR_UPGRADES`, and update `applyCycleOutcomes()` so the player gets the base `+2` Essence plus exactly one extra Essence if any resolved Rift in the cycle ended in victory.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Essence flow now rewards success modestly, delays permanent upgrades, and preserves steady baseline growth.</done>
</task>

<task type="auto">
  <name>Task 2: Prioritize new-faction troop offers until the player has a readable roster core</name>
  <files>src/engine/game.ts, src/engine/campaignSimulation.ts</files>
  <read_first>
    src/engine/game.ts
    src/engine/campaignSimulation.ts
    .planning/phases/03-full-run-campaign-balance/03-RESEARCH.md
    src/engine/campaign.test.ts
  </read_first>
  <acceptance_criteria>
    - `src/engine/game.ts` contains `const EARLY_FACTION_PRIORITY_LIMIT = 3`.
    - `src/engine/game.ts` contains `ownedFactionIds.size < EARLY_FACTION_PRIORITY_LIMIT`.
    - `src/engine/game.ts` contains `availableTroopUnlockIds.filter((troopUnlockId) => !ownedFactionIds.has(`.
    - `src/engine/campaignSimulation.ts` contains `ownedFactions`.
    - `src/engine/campaignSimulation.ts` contains `unusedEssenceByCycle`.
  </acceptance_criteria>
  <action>Add `EARLY_FACTION_PRIORITY_LIMIT = 3` to `src/engine/game.ts`. While the player owns fewer than 3 factions, build troop offers with bucket priority `[new faction, owned faction, owned unit type]`. Once the player owns 3 or more factions, restore the bucket priority to `[owned faction, owned unit type, new faction]`. Update `src/engine/campaignSimulation.ts` metrics to report `ownedFactions`, `ownedTroops`, and `unusedEssenceByCycle` so post-change sweeps can show whether early variety improved without starving the player.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Early progression reliably exposes new factions, but later runs can still deepen existing roster synergies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the new progression rules into deterministic campaign tests</name>
  <files>src/engine/campaign.test.ts</files>
  <read_first>
    src/engine/campaign.test.ts
    src/engine/game.ts
    src/engine/campaignSimulation.ts
    .planning/phases/03-full-run-campaign-balance/03-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/campaign.test.ts` contains `first-victory Essence bonus`.
    - `src/engine/campaign.test.ts` contains `upgrade offers stay hidden until three troops are owned`.
    - `src/engine/campaign.test.ts` contains `early troop offers prioritize new factions`.
    - `src/engine/campaign.test.ts` contains `upgrade claims cost two Essence`.
    - `npm run test` exits with only known unrelated failures if Phase 2 is still pending, and otherwise exits successfully.
  </acceptance_criteria>
  <action>Extend `src/engine/campaign.test.ts` with exact deterministic cases: one test that a cycle with at least one victory grants `+3` total Essence while an all-defeat cycle still grants `+2`; one test that `revealUpgradeOffer()` returns the unchanged state until exactly 3 troops are owned; one test that early troop offers surface an unowned faction in the first slot while the player owns fewer than 3 factions; and one test that `claimUpgradeOffer()` spends 2 Essence instead of 1.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The new economy and unlock rules are captured in regression tests and measurable through campaign sweeps.</done>
</task>

</tasks>

<verification>
Run `npm run test` after each task. If policy metrics show the new rules still overfeed or starve the player, adjust the concrete constants rather than weakening test coverage.
</verification>

<success_criteria>
The player economy now supports steady growth, delayed upgrade snowballing, and earlier roster variety without making the campaign opaque or brittle.
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-run-campaign-balance/03-03-SUMMARY.md`
</output>
