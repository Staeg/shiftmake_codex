---
phase: 02-intuitive-battlefield-roles
plan: 2
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/engine/simulationHarness.ts
  - src/engine/simulationHarness.test.ts
autonomous: true
requirements:
  - ROLE-01
  - ROLE-03
  - ROLE-04
  - ROLE-05
must_haves:
  truths:
    - Seed sweeps can quantify whether allied backline stays screened longer and enemy backline gets breached more reliably.
    - Chaff commitment is measurable across multiple seeds instead of only by manual replay reading.
    - Backline spacing behavior has an automated regression check separate from the tactical unit tests.
  artifacts:
    - path: src/engine/simulationHarness.ts
      provides: Role-behavior benchmark builders and replay-intent summary helpers.
      contains: buildRoleScenarioBattleInput
    - path: src/engine/simulationHarness.test.ts
      provides: Multi-seed regression tests for screen, breach, commitment, and spacing behavior.
      contains: describe('role behavior seed sweeps')
  key_links:
    - from: src/engine/simulationHarness.ts
      to: src/engine/battle.ts
      via: resolveBattle replay analysis
      pattern: roleIntent
    - from: src/engine/simulationHarness.test.ts
      to: src/engine/simulationHarness.ts
      via: deterministic seed-range assertions
      pattern: buildRoleScenarioBattleInput
---

<objective>
Add quantitative regression coverage for the new role behaviors so Phase 2 is not protected only by a few tactical snapshots.

Purpose: verify that the engine changes from Plan 01 hold up across many deterministic seeds.
Output: simulation-harness helpers for canonical role scenarios and seed-sweep tests covering screen, breach, commitment, and spacing outcomes.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
@.planning/phases/02-intuitive-battlefield-roles/02-VALIDATION.md
@src/engine/battle.ts
@src/engine/roleBehavior.test.ts
@src/engine/simulationHarness.ts
@src/engine/simulationHarness.test.ts

<interfaces>
From `src/engine/simulationHarness.ts`:

```ts
export interface SimulationMetrics {
  outcome: BattleOutcome;
  beatsToEnd: number;
  firstContactBeat: number | null;
  firstBacklineThreatBeat: number | null;
  backlineBreachRate: number;
}

export function buildSimulationBattleInput(
  seed: number,
  playerCombatants: ResolvedCombatantDefinition[],
  enemyCombatants: ResolvedCombatantDefinition[],
  saturation?: number,
  mutatorIds?: string[],
): BattleInput;

export function sweepBattleSeeds(makeInput: (seed: number) => BattleInput, seeds: number[]): SimulationSweepResult;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable role-benchmark builders and replay-intent counters</name>
  <files>src/engine/simulationHarness.ts</files>
  <read_first>
    src/engine/simulationHarness.ts
    src/engine/battle.ts
    src/engine/types.ts
    src/engine/roleBehavior.test.ts
    .planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/simulationHarness.ts` contains `export type RoleScenarioId =`.
    - `src/engine/simulationHarness.ts` contains `export function buildRoleScenarioBattleInput`.
    - `src/engine/simulationHarness.ts` contains `export function countRoleIntentSteps`.
    - `src/engine/simulationHarness.ts` contains `export function findFirstRoleIntentBeat`.
    - `src/engine/simulationHarness.ts` contains `'frontline-screen'`.
    - `src/engine/simulationHarness.ts` contains `'chaff-breach'`.
    - `src/engine/simulationHarness.ts` contains `'backline-spacing'`.
  </acceptance_criteria>
  <action>Add harness helpers that build three canonical Phase 2 benchmark battles: `frontline-screen`, `chaff-breach`, and `backline-spacing`. Reuse existing synthetic or catalog combatant builders instead of inventing a new harness DSL. Add replay-intent analyzers `countRoleIntentSteps()` and `findFirstRoleIntentBeat()` that read `BattleStep.metadata.roleIntent` from Plan 01 so multi-seed tests can measure screening, breach, commitment, and retreat behavior directly.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The harness can build repeatable role scenarios and summarize intent-bearing replay steps without manual log inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic seed-sweep regression tests for screen, breach, commitment, and spacing</name>
  <files>src/engine/simulationHarness.test.ts</files>
  <read_first>
    src/engine/simulationHarness.test.ts
    src/engine/simulationHarness.ts
    src/engine/roleBehavior.test.ts
    .planning/phases/02-intuitive-battlefield-roles/02-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/simulationHarness.test.ts` contains `describe('role behavior seed sweeps'`.
    - `src/engine/simulationHarness.test.ts` contains `frontline-screen`.
    - `src/engine/simulationHarness.test.ts` contains `chaff-breach`.
    - `src/engine/simulationHarness.test.ts` contains `backline-spacing`.
    - `src/engine/simulationHarness.test.ts` contains `countRoleIntentSteps`.
    - `npm run test` exits successfully.
  </acceptance_criteria>
  <behavior>
    - Frontline-screen sweeps prove allied backline threat is delayed and screen/frontline intent appears consistently.
    - Chaff-breach sweeps prove breach and hold-backline intents both appear across the chosen seed range.
    - Backline-spacing sweeps prove retreat-range or advance-range spacing logic appears without early same-hex collapse.
  </behavior>
  <action>Add a dedicated `describe('role behavior seed sweeps')` block that runs each benchmark over a fixed seed range such as `createSeedRange(8, 200)`. Assert concrete ratios instead of vague expectations: the frontline scenario must emit `screen-frontline` intent in most seeds and keep `firstBacklineThreatBeat` null or later than first contact; the chaff scenario must emit both `breach-backline` and `hold-backline` across the sweep; the backline-spacing scenario must emit `retreat-range` or `advance-range` and avoid immediate same-hex breach in the early beats. Keep the thresholds strict enough to catch regressions but tied to deterministic replay signals, not manual judgment.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Phase 2 behavior is protected by seed-sweep tests that exercise the new heuristics over multiple seeds and not just one curated replay.</done>
</task>

</tasks>

<verification>
Run `npm run test` after each task. If the chosen thresholds are brittle, tighten the scenario construction in `buildRoleScenarioBattleInput()` rather than weakening Phase 2 expectations.
</verification>

<success_criteria>
The simulation harness can automatically detect regressions in screening, backline breach commitment, and ranged spacing over deterministic seed sweeps.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intuitive-battlefield-roles/02-02-SUMMARY.md`
</output>
