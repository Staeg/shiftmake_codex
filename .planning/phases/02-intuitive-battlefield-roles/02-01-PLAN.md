---
phase: 02-intuitive-battlefield-roles
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/engine/battle.ts
  - src/engine/roleBehavior.test.ts
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - ROLE-03
  - ROLE-04
  - ROLE-05
  - ROLE-06
must_haves:
  truths:
    - Frontline scenarios show allied backline staying screened by engaged blockers before direct enemy access opens.
    - Frontline units switch to reachable enemy backline targets when no enemy frontline or chaff target remains.
    - Chaff scenarios show a legal breach into enemy backline and continued backline-focused behavior on later turns.
    - Backline units increase or preserve spacing when a legal retreat hex exists instead of choosing a random adjacent hex.
    - Replay steps expose role-intent metadata and readable reason text from the engine itself.
  artifacts:
    - path: src/engine/types.ts
      provides: Typed replay role-intent metadata contract.
      contains: export type RoleIntentId
    - path: src/engine/battle.ts
      provides: Role-specific target and movement scoring with chaff commitment memory.
      contains: pickFrontlineObjective
    - path: src/engine/roleBehavior.test.ts
      provides: Tactical scenario tests for frontline, chaff, backline, and replay readability.
      contains: describe('role behavior')
  key_links:
    - from: src/engine/battle.ts
      to: src/engine/types.ts
      via: BattleStep.metadata role-intent fields
      pattern: roleIntent
    - from: src/engine/roleBehavior.test.ts
      to: src/engine/battle.ts
      via: resolveBattle assertions on movement, engagement, and replay messages
      pattern: resolveBattle
---

<objective>
Lock the phase's engine contract: role heuristics, transient commitment state, and replay intent metadata all live in the pure battle runtime.

Purpose: satisfy the core Phase 2 role requirements without leaking combat reasoning into UI code.
Output: typed replay role-intent metadata, deterministic role heuristics in `battle.ts`, and dedicated scenario coverage in `roleBehavior.test.ts`.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
@.planning/phases/02-intuitive-battlefield-roles/02-VALIDATION.md
@TECHNICAL.md
@src/engine/types.ts
@src/engine/battle.ts
@src/engine/battle.test.ts

<interfaces>
From `src/engine/types.ts`:

```ts
export type RoleId = 'frontline' | 'chaff' | 'backline';

export interface BattleStep {
  index: number;
  kind: BattleStepKind;
  actorIds: string[];
  targetIds: string[];
  message: string;
  snapshot: BattleStateSnapshot;
  metadata?: Record<string, number | string | boolean>;
}
```

From `src/engine/battle.ts`:

```ts
export function resolveBattle(input: BattleInput): BattleReplay;
export function resolveDebugBattle(input: BattleDebugInput): BattleReplay;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add role-behavior scenario tests before touching the runtime</name>
  <files>src/engine/roleBehavior.test.ts</files>
  <read_first>
    .planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
    .planning/phases/02-intuitive-battlefield-roles/02-VALIDATION.md
    src/engine/battle.ts
    src/engine/battle.test.ts
    src/engine/types.ts
  </read_first>
  <acceptance_criteria>
    - `src/engine/roleBehavior.test.ts` contains `describe('role behavior'`.
    - `src/engine/roleBehavior.test.ts` contains `frontline screens allied backline before enemy reaches it`.
    - `src/engine/roleBehavior.test.ts` contains `frontline falls back to reachable enemy backline targets`.
    - `src/engine/roleBehavior.test.ts` contains `chaff breaches enemy backline and stays committed`.
    - `src/engine/roleBehavior.test.ts` contains `backline preserves distance when a retreat hex exists`.
    - `src/engine/roleBehavior.test.ts` contains `replay metadata exposes role intent and reason codes`.
  </acceptance_criteria>
  <behavior>
    - Test 1: Frontline intercepts or occupies the contested hex before an enemy reaches allied backline.
    - Test 2: Frontline redirects to a reachable enemy backline target when no frontline/chaff target is available.
    - Test 3: Chaff breaches into enemy backline and keeps choosing committed backline behavior across later steps.
    - Test 4: Backline increases or preserves enemy distance when a legal retreat option exists.
    - Test 5: Replay `move` or `engage` steps expose `roleIntent` and `reasonCode`.
  </behavior>
  <action>Create `src/engine/roleBehavior.test.ts` with deterministic battle inputs built from existing engine helpers. Use fixed seeds and exact replay-step assertions instead of broad outcome-only checks. Keep the scenarios narrow: one frontline screen case, one frontline fallback case, one chaff breach-and-hold case, one backline retreat case, and one replay metadata case. Do not modify existing broad regression coverage in `battle.test.ts`.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Dedicated role-behavior tests fail on the current generic heuristics and describe the exact Phase 2 behavior contract.</done>
</task>

<task type="auto">
  <name>Task 2: Define typed replay role-intent metadata in the engine contracts</name>
  <files>src/engine/types.ts</files>
  <read_first>
    src/engine/types.ts
    src/engine/battle.ts
    .planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/types.ts` contains `export type RoleIntentId =`.
    - `src/engine/types.ts` contains `export interface BattleStepMetadata`.
    - `src/engine/types.ts` contains `roleIntent?: RoleIntentId;`.
    - `src/engine/types.ts` contains `reasonCode?: string;`.
    - `src/engine/types.ts` contains `targetRole?: RoleId;`.
    - `src/engine/types.ts` contains `metadata?: BattleStepMetadata;`.
  </acceptance_criteria>
  <action>Add a typed replay metadata contract with the exact intent codes `screen-frontline`, `fallback-backline`, `breach-backline`, `hold-backline`, `retreat-range`, and `advance-range`. Introduce `BattleStepMetadata` in `src/engine/types.ts` and wire `BattleStep.metadata` to that type so replay consumers can rely on `roleIntent`, `reasonCode`, `targetRole`, `targetHexQ`, and `targetHexR` without parsing freeform strings.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The replay contract can carry role-intent data with named fields instead of an untyped generic record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace generic pursuit and retreat decisions with role-specific heuristics</name>
  <files>src/engine/battle.ts</files>
  <read_first>
    src/engine/battle.ts
    src/engine/types.ts
    src/engine/roleBehavior.test.ts
    .planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
    TECHNICAL.md
  </read_first>
  <acceptance_criteria>
    - `src/engine/battle.ts` contains `pickFrontlineObjective`.
    - `src/engine/battle.ts` contains `pickChaffObjective`.
    - `src/engine/battle.ts` contains `scoreRetreatHex`.
    - `src/engine/battle.ts` contains `emitRoleIntentStep`.
    - `src/engine/battle.ts` contains `'screen-frontline'`.
    - `src/engine/battle.ts` contains `'fallback-backline'`.
    - `src/engine/battle.ts` contains `'breach-backline'`.
    - `src/engine/battle.ts` contains `'hold-backline'`.
    - `src/engine/battle.ts` contains `'retreat-range'`.
    - `npm run test` exits successfully.
  </acceptance_criteria>
  <behavior>
    - Frontline prioritizes blocking and same-hex interception before general nearest-target pursuit.
    - Frontline falls through to reachable enemy backline when no frontline/chaff option exists.
    - Chaff prioritizes enemy backline spill paths and remembers a backline commitment across turns.
    - Backline scores retreat hexes by threat distance and does not choose random retreats when better space exists.
    - Replay move/engage steps emit intent metadata and plain-English reason strings.
  </behavior>
  <action>Refactor `executeTurnActions()` around explicit role helpers rather than generic `pursue()` and random `retreat()`. Add battle-only commitment memory on the internal unit/runtime state for ROLE-04, clear it when the target/hex is no longer legal, and emit role-intent metadata through a small `emitRoleIntentStep()` helper so movement and engage steps stay authoritative. Keep all logic inside `src/engine/battle.ts`; do not add UI state, DOM dependencies, or renderer-side interpretation.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The battle runtime satisfies the new scenario tests and exposes replay-visible intent directly from the engine.</done>
</task>

</tasks>

<verification>
Run `npm run test` after each task and once again after the plan finishes. If a new scenario test is flaky, fix the scenario construction rather than weakening assertions.
</verification>

<success_criteria>
Phase 2 now has an engine-owned role contract: tactical scenario tests exist, battle steps carry typed role-intent metadata, and frontline/chaff/backline decisions are no longer driven by generic nearest-target logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intuitive-battlefield-roles/02-01-SUMMARY.md`
</output>
