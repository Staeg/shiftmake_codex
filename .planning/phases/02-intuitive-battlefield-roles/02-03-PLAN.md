---
phase: 02-intuitive-battlefield-roles
plan: 3
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/ui/inspectText.ts
  - src/ui/EventLog.svelte
  - src/ui/battleRecap.ts
  - src/ui/battleRecap.test.ts
autonomous: true
requirements:
  - ROLE-06
must_haves:
  truths:
    - Replay consumers display role-intent output directly from engine metadata instead of inferring behavior client-side.
    - Tooltip copy matches the actual Phase 2 behavior contract for frontline, chaff, and backline.
    - Recap output surfaces whether a troop screened, breached, held backline, or preserved distance.
  artifacts:
    - path: src/ui/EventLog.svelte
      provides: Read-only display of replay role-intent badges.
      contains: roleIntentLabel
    - path: src/ui/battleRecap.ts
      provides: Troop recap summaries derived from replay metadata.
      contains: roleSummary
    - path: src/ui/inspectText.ts
      provides: Role descriptions aligned with engine behavior.
      contains: blocks access to allied backline
    - path: src/ui/battleRecap.test.ts
      provides: Tests for metadata-driven recap summaries.
      contains: roleSummary
  key_links:
    - from: src/ui/EventLog.svelte
      to: src/engine/types.ts
      via: BattleStep.metadata.roleIntent consumption
      pattern: step.metadata?.roleIntent
    - from: src/ui/battleRecap.ts
      to: src/engine/battle.ts
      via: replay step metadata summarization
      pattern: roleIntent
---

<objective>
Make the new role behavior readable in the replay UI without moving any game logic out of the engine.

Purpose: satisfy ROLE-06 by turning engine-emitted role-intent metadata into clear, thin replay presentation.
Output: updated role copy, event-log badges, recap summaries, and tests that verify read-only replay consumption.
</objective>

<execution_context>
@C:/Users/staeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/staeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
@TECHNICAL.md
@src/engine/types.ts
@src/ui/inspectText.ts
@src/ui/EventLog.svelte
@src/ui/battleRecap.ts
@src/ui/battleRecap.test.ts

<interfaces>
From `src/ui/battleRecap.ts`:

```ts
export interface BattleRecapTroopEntry {
  side: SideId;
  troopLabel: string;
  damageDone: number;
  healingDone: number;
  kills: number;
  units: BattleRecapUnitEntry[];
}

export function buildBattleRecap(replay: BattleReplay): BattleRecapTroopEntry[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align role copy with the new engine behavior contract</name>
  <files>src/ui/inspectText.ts</files>
  <read_first>
    src/ui/inspectText.ts
    src/engine/types.ts
    .planning/phases/02-intuitive-battlefield-roles/02-RESEARCH.md
    .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - `src/ui/inspectText.ts` contains `blocks access to allied backline`.
    - `src/ui/inspectText.ts` contains `spills into enemy backline`.
    - `src/ui/inspectText.ts` contains `preserve distance when geometry allows`.
    - `src/ui/inspectText.ts` still contains `export function formatRoleExact`.
  </acceptance_criteria>
  <action>Rewrite `formatRoleExact()` so each role description mirrors the new engine behavior from Plan 01. Use direct, player-facing wording: frontline blocks access to allied backline and falls through to enemy backline when the lane opens; chaff spills into enemy backline and stays committed there; backline preserves distance when geometry allows. Do not add speculative behavior not implemented in the engine.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Tooltip-level role descriptions match the actual Phase 2 runtime rules and replay vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface role-intent metadata in recap summaries and cover it with tests</name>
  <files>src/ui/battleRecap.ts, src/ui/battleRecap.test.ts</files>
  <read_first>
    src/ui/battleRecap.ts
    src/ui/battleRecap.test.ts
    src/engine/types.ts
    src/engine/roleBehavior.test.ts
  </read_first>
  <acceptance_criteria>
    - `src/ui/battleRecap.ts` contains `roleSummary`.
    - `src/ui/battleRecap.ts` contains `Held line`.
    - `src/ui/battleRecap.ts` contains `Broke through`.
    - `src/ui/battleRecap.ts` contains `Kept range`.
    - `src/ui/battleRecap.test.ts` contains `roleSummary`.
    - `npm run test` exits successfully.
  </acceptance_criteria>
  <behavior>
    - Troops that emit `screen-frontline` or `fallback-backline` summarize as holding the line.
    - Troops that emit `breach-backline` or `hold-backline` summarize as breaking through.
    - Troops that emit `retreat-range` or `advance-range` summarize as keeping range.
  </behavior>
  <action>Extend `BattleRecapTroopEntry` with a `roleSummary: string[]` field derived purely from replay step metadata. In `buildBattleRecap()`, aggregate per-troop role intents from `BattleStep.metadata.roleIntent` and map them to exact recap labels `Held line`, `Broke through`, and `Kept range`. Update `src/ui/battleRecap.test.ts` to cover the new summary mapping with a synthetic replay that includes the Phase 2 intent codes.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Battle recaps expose role-intent outcomes from replay metadata and the helper is covered by deterministic tests.</done>
</task>

<task type="auto">
  <name>Task 3: Render role-intent badges in the event log without adding UI-owned combat logic</name>
  <files>src/ui/EventLog.svelte</files>
  <read_first>
    src/ui/EventLog.svelte
    src/engine/types.ts
    src/ui/battleRecap.ts
    TECHNICAL.md
  </read_first>
  <acceptance_criteria>
    - `src/ui/EventLog.svelte` contains `roleIntentLabel`.
    - `src/ui/EventLog.svelte` contains `step.metadata?.roleIntent`.
    - `src/ui/EventLog.svelte` contains `intent-badge`.
    - `src/ui/EventLog.svelte` still renders `step.message`.
  </acceptance_criteria>
  <action>Add a small read-only mapper such as `roleIntentLabel()` inside `EventLog.svelte` that converts engine intent codes into short badges like `Hold line`, `Break through`, and `Keep range`. Render the badge only when `step.metadata?.roleIntent` exists, keep the existing message as the primary text, and do not infer role behavior from coordinates or other UI-side heuristics.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>The event log visibly surfaces engine-authored role intent while keeping presentation thin and deterministic.</done>
</task>

</tasks>

<verification>
Run `npm run test` after each task. Manual replay review can happen later, but the code in this plan must stay purely metadata-driven and fully covered by automated checks.
</verification>

<success_criteria>
Replay consumers now present the engine's role-intent output clearly enough that players can understand frontline, chaff, and backline behavior without reverse-engineering coordinates.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intuitive-battlefield-roles/02-03-SUMMARY.md`
</output>
